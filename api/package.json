{
  "name": "@netcoin/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "parity": "npm run build && node dist/run-parity.js",
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/cors": "latest",
    "fastify": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "typescript": "latest",
    "@types/node": "latest"
  }
}
