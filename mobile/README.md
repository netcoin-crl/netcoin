# mobile

Mobile wallet upgrade lane.

Start with React Native TypeScript for speed, then consider Swift/Kotlin if usage demands native performance.
Must share wallet-core vectors and signed-envelope SDK behavior.
